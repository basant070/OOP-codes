# OOP-codes
This repository stores C++ codes
variables, data types, operators, loops, functions, class and object declaration and definition, inheritance 
polymorphism, exception handling, file handling,  etc.

TOPIC 0: BASICS IN C++

Data types in C++
        Primitive types: int, short, long, long long, unsigned int, float, double, long double, char, unsigned char, wchar_t, bool
        Derived types: arrays, pointers, references, function pointers
        User-defined types: struct, union, enum, class

Operators in C++
        Arithmetic:   + - * / %
        Relational:   == != > < >= <=
        Logical:      && || !
        Bitwise:     & | ^ ~ << >>
        Assignment:  = += -= *= /= %= <<= >>= &= |= ^=
        Increment/Decrement : ++ -- (pre and post)
        Ternary:      ?:
        Comma:  ,
        sizeof
        Pointer operators:  & *
        Scope resolution:  ::
        Dynamic memory:    new, delete
 Function 
    - function call by value
    - function call by reference 
    - function return by reference
    - function overloading 

 DMA :
 Dynamic Memory Allocation: Dynamic Memory Allocation is the process of allocating memory at runtime (while the program is running), rather than at compile time. This lets your program request exactly the memory it needs, when it needs it, and release it when done.

Function : 
Function declaration and definition 
Function call by value and reference 
Function return by value and reference
=========================================================================================
